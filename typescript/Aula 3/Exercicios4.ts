
let number: number = parseInt("digiteo numero que deseja ver a tabuada: ")

for (let contador = 1; contador <= 10; contador++) {
  console.log(`${number} x ${contador} = ${number * contador}`); 
}